# Javis 开发方法论 — Claude 工作流执行引擎

> 版本：EXEC-ENGINE-2026-08-09
> 定位：MASTER-PLAN 的**执行层**——用什么工作流去落地 P0→P5 每一阶段。这是"方式"而非"内容"，是 Javis 计划的运行机制。

---

## 一、核心思想：把 Javis 当成"用它自己的理念来建造"

Javis 的愿景是 **Memory + Reasoning + Planning + Action + Reflection** 的认知循环、多 Agent 协作、自我进化。
建造 Javis 的过程，就应该**使用同一套模式**：记忆沉淀、多代理并行、规划执行、反思学习。

这构成三层结构：
```
[目标层]  Javis AI OS 完整计划（MASTER-PLAN P0→P5）
[方法层]  本文档：Claude 工作流执行引擎（怎么干）
[工具层]  技能/代理/Workflow 编排（用什么干）
```

---

## 二、可用的工作流资产（已核验，非假设）

### 2.1 技能层（Skill，按需触发）
| 技能 | 用于 Javis 哪个阶段 | 触发场景 |
|---|---|---|
| `superpowers`（14 工作流） | **贯穿所有阶段** | 每项开发任务开始时自动路由：创意→计划→TDD→调试→并行→审查→验证→分支 |
| `auto-file-analyzer` | P1 发布审计 | 自动检测文件类型、与 Javis 对比、出结构化报告 |
| `auto-git-sync` | P1 发布纪律 | 定时同步（需用户授权，暂不启用） |
| `consolidate-memory` | P2 记忆系统 | 记忆整理（Javis 的记忆也用它） |
| `frontend-design` / `dataviz` | P4 数字人 UI | 视觉方向、图表规范 |
| `claude-api` | 模型接入 | LLM 相关：模型 ID/定价/流式/工具调用参考 |
| `pdf/docx/pptx/xlsx` | P1 报告、P5 文档 | 生成验收报告、技术文档 |

### 2.2 代理层（Agent，子代理）
| 类型 | 用于 Javis 哪个阶段 |
|---|---|
| `claude`（通用） | 任何任务 |
| `general-purpose` | 复杂问题研究、多步任务（P3 反思研究） |
| `Explore` | 只读搜索定位代码（P0-P5 各阶段的代码勘探） |
| `Plan` | 软件架构设计（P2 记忆 schema、P4 3D 架构、P5 权限模型） |
| `claude-code-guide` | 需要 Claude 能力参考时 |

### 2.3 编排层（Workflow tool，多代理确定性编排）
本会话已在用：**6 个设计代理并行读真实源码 + 1 个批判代理**。
模式：
- `parallel`（屏障同步）：各阶段独立设计后合并
- `pipeline`（逐项流水）：如"记忆 schema → 检索实现 → 集成 → 测试"
- `agent(schema)`：强制结构化输出，effort 控制深度（xhigh=深度设计）
- **批判代理**：跨阶段一致性、依赖、遗漏审查

### 2.4 执行纪律（从 Codex SDD 提炼 + 本会话铁律）
| 纪律 | 说明 |
|---|---|
| **TDD 红绿循环** | 先写失败测试锁定缺口 → 实现 → 转绿。已验证：SNR 指标、TTS 失败提示 |
| **隔离区开发** | 只写 `G:\Javis-worktrees`，C 盘只读，D 盘只验收 |
| **独立提交** | 每项修复一提交，禁止混提交（语音/UI/安装器/打包分开） |
| **三层验证** | 基线验证 + 聚焦验证 + 全量验证（Codex SDD 模式） |
| **审查抓隐性缺陷** | 竞态、吞错、迟到事件复活、"测试全绿仍失效" |
| **真实运行驱动** | 一切以真实命令输出为准，需真实验收的标注"需 D 盘验收" |

---

## 三、分阶段执行引擎映射（Claude 工作流 × Javis 阶段）

### P0 语音闭环 → 用「TDD + 真实验收」驱动
```
1. 先跑基线：全量 Python + 前端（本会话已跑：语音 50 passed、前端 119 passed）
2. 每修一个缺陷：红线测试 → 实现 → 绿线
3. D 盘真实验收：6 门槛逐条跑，如实记录（Claude 只记录真实结果）
4. 用 `auto-file-analyzer` 审审计报告
```

### P1 可信发布 → 用「Plan 代理 + Explore 勘探」驱动
```
1. Explore 勘探：.gitignore 覆盖情况、构建脚本依赖、12 个失败根因
2. Plan 代理：设计构建流水线 + 版本清单 schema
3. 执行：合并分支 → 一条命令构建 → 产出报告
```

### P2 记忆系统 → 用「Plan 代理 + Workflow 并行」驱动
```
1. Plan 代理：设计 MemoryManager schema（五层 + 治理 + 检索）
2. Workflow 并行：记忆层实现 / 治理实现 / 检索实现 三个代理并行
3. 批判代理：跨层一致性审查（schema 是否统一、检索是否接上）
4. 集成测试：五层读写 + 治理 + 检索 + 与 agent 循环
```

### P3 认知进化 → 用「superpowers 计划/执行 + 批判代理」驱动
```
1. superpowers：任务拆解（Planner 增强 / Reflection / Multi-Agent / Evolution）
2. 每子项：TDD + 独立提交
3. 批判代理：跨模块一致性（四闭环是否打通）
```

### P4 数字生命体 → 用「Plan 代理 + 前端设计技能」驱动
```
1. Plan 代理：3D 引擎选型评估（Three.js/Unity/wgpu）+ 状态→表现映射架构
2. frontend-design：视觉方向
3. Explore：pet/ 桌宠现有代码复用点
4. 原型 → 验收：认知状态驱动表现，非状态灯
```

### P5 全域管家 → 用「Explore + 批判代理」驱动
```
1. Explore：tools/、control/、gateway/ 现有能力盘点
2. Plan 代理：权限模型（动态授权）+ 双孪生 schema
3. 批判代理：安全审查（高风险操作人工确认）
```

---

## 四、执行循环（每阶段通用）

```
┌─────────────────────────────────────────┐
│  1. 建任务清单（TaskCreate 追踪）        │
│  2. 勘探现状（Explore 代理，读真实代码） │
│  3. 设计方案（Plan 代理，多方式+推荐）   │
│  4. 红线测试 → 实现 → 绿线（TDD）       │
│  5. 独立提交（逐项可回退）              │
│  6. 批判审查（竞态/吞错/遗漏）          │
│  7. 真实验收（D 盘，如实记录）          │
│  8. 反思总结 → 更新记忆/计划            │
│  ↺                                        │
└─────────────────────────────────────────┘
```

---

## 五、记忆沉淀（持续）

每次阶段完成，把非显而易见的东西写入记忆：
- 技术发现（如"TTS 失败被静默吞掉"）
- 工作流教训（如"测试全绿仍失效"的审查点）
- 用户偏好（如"不虚构通过"）
- 阶段成果（如"P0 语音 6 门槛 D 盘通过"）

用 `consolidate-memory` 定期整理。

---

## 六、本执行引擎在 MASTER-PLAN 中的位置

```
MASTER-PLAN（目标）: P0→P5 完整路线 + 各阶段多方案
        ↑ 参考
EXEC-ENGINE（方法）: 本文档，Claude 工作流怎么执行每阶段
        ↑ 驱动
工具层: superpowers 技能 + 代理 + Workflow 编排 + TDD 纪律
```

> 一句话：**用 Javis 的认知循环理念来建造 Javis**——计划是"记忆层"，本引擎是"认知层"，工具是"行动层"，每阶段结束都"反思并沉淀"。
