# Javis AI OS 推进计划 — 三阶段（P0→P1→P2）深度规划

> 版本：PLAN-2026-08-09 / 作者：Caleb-Top + Claude
> 原则：**先稳定，再发布，后进化**。不做任何修改时仅做核验；每一改动都基于真实运行结果，不虚构"通过"。

---

## 〇、当前真实基线（核验于 2026-08-09，非假设）

### 目录角色约定（已确认）
| 目录 | 角色 | 现状 |
|---|---|---|
| `G:\Javis` | 唯一源码/编译/依赖/缓存/构建环境 | main 分支；有未跟踪大目录（见下） |
| `G:\Javis-worktrees` | 隔离开发工作树（一级待修改区） | `codex-javis-core-stabilization`、`voice-p0-reliability` 已存在 |
| `D:` | 模拟真实用户电脑，仅安装/运行/升级/卸载验收 | v3.0.0 安装包 + 安装测试目录 + AppData 运行时 |
| 用户配置/记忆/运行数据 | 若要求驻留 G 盘 → 通过应用配置实现 | 不擅自依赖 |

### 关键文件证据（已实测）
- **P0 语音修复**：C 盘 `work/javis-p0-voice-fix` 分支含 8 个修复提交，已 1:1 搬运到 `G:\Javis-worktrees\voice-p0-reliability`（哈希逐文件一致），已补 3 个新修复提交（SNR 指标、设备下拉、TTS 失败提示），Python 语音 50 passed + 前端 119 passed。
- **D 盘运行时真实状态**：`AppData/Local/local.javis.desktop/runtime`（26963 文件 / 2GB），`runtime-version.json` 创建于 08-05，日志最后活动 08-05。→ **真实安装端仍是旧版，P0 修复未生效于真实环境**。
- **G 盘未跟踪大目录**（git status 可见）：`models/` 144M、`tools/python-runtime-3.11/` 4.2G、`artifacts/` 16G、`tools/cvu_data/` 3.6G。`.gitignore` **未覆盖**这些（只覆盖了 tools/rust、tools/nodejs、venv、__pycache__ 等）。
- **12 个预存测试失败**（与 P0 无关，main 即失败）：`test_full_offline_installer`（离线安装器/R1 模型）、`test_p0_runtime`（身份记忆）、`test_app_build_preflight`（离线就绪）、`test_app_v1_release`（manifest）。→ 属环境/基线问题，不是本次引入。

---

## 一、总体判断：项目当前处在"过渡期"

不是"没有进度"，而是**进度与真实环境脱节**：
1. 修复在 C 盘隔离区 → 已搬回 worktree → **尚未合入 main / 未打包 / 未装 D 盘**。
2. 测试"全绿"但**真实设备未验收**（Whisper 识别率、麦克风占用、30 轮连续对话都需要真实环境）。
3. 发布基线不干净（24GB 未跟踪物、12 个预存失败、历史混提交）。

因此计划的**第一目标不是加功能，而是"让真实环境跑起来并闭环"**。

---

## 二、P0 阶段：语音闭环（剩余工作 → 真实验收）

> P0 目标已在本 worktree 完成"代码层"。剩余是**真实设备验收**与**发现的问题闭环**。

### P0-A. 真实安装端验收（D 盘，最高优先级）
验收门槛（逐条，缺一不可）：
1. 连续语音对话 30 轮，无永久"思考中"。
2. 关闭窗口 / 隐藏托盘 / 重开界面 / 后端重连 各 10 次，无麦克风占用残留（`microphone stream belongs to another conversation` 不复现）。
3. 拔网 / API 失败 / Ollama 未启动 / 麦克风断开 → 均有明确提示。
4. 20 条固定中文语料不得出现空识别；近讲字错率 < 20%。
5. 打断响应 < 500ms。
6. 完成 / 失败 / 取消后 1 秒内恢复可继续说话。

**落地动作**：
- 先合并 `fix/p0-voice-reliability` 到 `G:\Javis` main（P1 中做，但验收依赖它）。
- 在 D 盘安装新构建，逐条跑验收，**如实记录每条通过/失败**。
- 失败项 → 回到 worktree 补修复 → 重新构建 → 复验。

### P0-B. 固定中文语料校准（未做，需真实模型）
- 现状：`voice/stt.py` 已配置 `language: zh`，`faster-whisper-base` 模型在 G 盘，但**没有固定中文语料的自动校准测试**。
- 落地：在 D 盘验收时，录制 20 条固定中文短句（覆盖数字、命令、常见指令），存为 wav 语料，跑 Whisper 转写，记录字错率；作为回归基线存档。
- 说明：真实人声识别率**无法在无麦克风的构建环境伪造通过**，必须 D 盘真实验收。这是我坚持不"宣称通过"的原因。

### P0-C. 已知风险点
- **TTS 播报失败提示**：已修（前端 `.catch` 显示原因），需在 D 盘验证"TTS 失败后能回到聆听"。
- **`request.accepted` 超时 / 云首字 15s / 总时限 60s**：代码已实现，需真实网络下验证超时后界面退出"思考"。

---

## 三、P1 阶段：可信发布流程（建立发布纪律）

### P1-1. 修复发布基线（必须先做）
- 目标：`G:\Javis` main 工作树干净、可追踪、可复现构建。
- 动作：
  1. `.gitignore` 补齐：`models/`、`tools/python-runtime-3.11/`、`tools/cvu_data/`、`tools/yolo/`、`artifacts/`（构建产物不进版本库）。**注意**：先确认这些目录是否需要保留于 G 盘（模型/运行时是构建依赖，artifacts 是产物）——正确做法是**忽略**而非删除。
  2. 将 `fix/p0-voice-reliability` 合入 `main`（用 merge --no-ff，保留完整历史；逐提交 review）。
  3. 记录当前 main 的源码提交号 + 运行时哈希（`runtime-version.json` 的 sha256）。

### P1-2. 建立构建流水线（G 盘只编译）
- 现状：构建脚本分散（`scripts/build_javis_app_v3.ps1`、`javis_release_runtime.py`、`finalize_javis_release.py` 等）。
- 目标：一条命令从 clean main → 产物 + 版本清单。
- 产物必须附带：
  - 源码提交号（git rev-parse HEAD）
  - 运行时哈希（javis-runtime.zip sha256）
  - 验收报告（D 盘真实验收结果）
- 打包前自动跑：Python 全量测试（除 12 个环境性失败外）+ 前端全量 + 语音套件，**不绿不打包**。

### P1-3. 处理 12 个预存失败（诚实归类）
- 逐条核验失败根因，分三类处理：
  - **环境性**（如缺 R1 模型文件、离线安装器需要特定目录）→ 修复测试的环境准备，或标记为"需要真实模型/安装环境"。
  - **真 Bug** → 修代码。
  - **过期断言**（代码已演进但测试未更新）→ 更新测试。
- 每类必须有真实证据（运行日志 / 文件存在性 / 断言内容），不许含糊跳过。

### P1-4. 提交纪律（固化）
- 每个修复独立提交，禁止混提交（模型安装器 / UI / 语音 / 打包分开）。
- G 盘只负责编译更新；D 盘只负责真实安装测试。
- P0 验收通过前，不宣称"安装包完成"。

---

## 四、P2 阶段：记忆系统（分层实施，语音稳定后启动）

> 现状基础已存在（`memory/` 目录：episodic / semantic / procedural / consolidation / session_db 等；`knowledge/brain.py`；`utils/memory.py`）。**不是从零建，而是按目标重构对齐**。

### P2-A. 统一记忆入口（五层记忆）
| 层 | 类型 | 现状 | 目标 |
|---|---|---|---|
| L1 | 事件记忆 Event | `memory/episodic.py`、`session_events.sqlite` | 统一事件入口，带来源/时间/重要性 |
| L2 | 语义记忆 Semantic | `memory/semantic.py`、`knowledge/brain.py`（Fact/Experience） | 事实/知识，带置信度/来源 |
| L3 | 偏好记忆 Preference | `memory/profiles/`、`utils/memory.py`（save_profile） | 用户偏好，带有效期 |
| L4 | 策略记忆 Strategy | 部分在 `memory/procedural.py`（工作流物化） | 行为策略，带触发条件 |
| L5 | 身份记忆 Identity | `knowledge/brain.py`、prompt_builder | 用户数字人格模型 |

**核心动作**：把分散的读写收敛为一个统一 `MemoryManager` API（事件/语义/偏好/策略/身份），**不重写已有模块**，而是加一层薄门面 + 统一 schema。

### P2-B. 记忆治理（每条记忆的元数据）
每条记忆必须带：
- 来源（source：user / system / self / tool）
- 时间（created_at / updated_at）
- 置信度（confidence 0~1）
- 有效期（expires_at，可空=长期）
- 隐私级别（private / local / shareable）

能力：查看 / 修改 / 遗忘（软删或硬删）/ 导出（JSON）。

### P2-C. 检索组合（先 SQLite/FTS + 向量，后 Graph）
- **第一阶段**：现有 `session_db.py`（SQLite/FTS）+ 向量检索（faster-whisper 已有；embedding 可用本地模型）。
- **第二阶段**：关系复杂后引入 Memory Graph（Neo4j 或 sqlite-graph），做关联检索。
- 原则：**不提前上 Graph**，先用够用的方案跑通，证据驱动升级。

### P2-D. 认知内核（Agent Loop 统一）
- 现状：`core/agent.py` 已有 live_fast 路径、工具按需加载、interaction_mode 分流。
- 目标：规划→行动→观察→反思 形成统一循环，记忆在循环各阶段读写。
- 顺序：先做"行动后记忆写入 + 反思合并"，再做"规划时记忆召回"。

### P2-E. Critic / Verifier（重要操作前验证、后检查）
- 现状：`core/tool_guardrails.py` 有权限/参数/路径检查。
- 目标：补"执行后结果校验"——重要操作（文件修改/系统命令/注册表）执行后验证结果与预期，失败自动回滚或提示。
- 放后期：数字孪生、Multi-Agent、Evolution、3D 数字人（**不抢占语音稳定性和权限安全优先级**）。

---

## 五、时间与优先级（关键路径）

```
[1] P1-1 合并 P0 → main（最快解锁 D 盘验收）
[2] P1-2 一条命令构建 v3.0.1 + 版本清单
[3] P0-A  D 盘真实验收（30 轮语音 / 断线 / 提示 / 中文语料 / 打断 / 恢复）
[4] P1-3 修复 12 个预存失败（三类归类）
[5] P1-4 提交纪律固化（.gitignore + 独立提交规范）
[6] P2-A/B 记忆统一入口 + 治理（在语音验收通过后启动）
[7] P2-C/D/E 检索组合 → 认知内核 → Critic（P2 内递增）
[8] 后期：Memory Graph / 数字孪生 / Multi-Agent / Evolution / 3D 数字人
```

**关键路径**：1 → 2 → 3（P0 验收通过 = P1 完成标志）→ 6 → 7 → 8。

---

## 六、风险与决策记录

| 风险 | 应对 |
|---|---|
| D 盘验收可能暴露真实语音缺陷 | 验收失败 → 回 worktree 修复 → 重建 → 复验，不绕过 |
| 12 个预存失败可能含真 Bug | 逐条归因，用证据分类，不打包混修 |
| 24GB 未跟踪物污染 git | `.gitignore` 忽略而非删除（模型/运行时是构建依赖） |
| C 盘 Codex 隔离区权限/所有权 | 只用只读 git 读取，改动一律经 worktree |
| 远程 token 泄漏风险 | 不 push；worktree remote 含 token 但仅本地读取 |

---

## 七、立即执行清单（下一步，待批准）

1. **修改 `.gitignore`**（在 worktree 改好再合回，避免污染 main）：忽略 models/、tools/python-runtime-3.11/、tools/cvu_data/、tools/yolo/、artifacts/。
2. **合并 `fix/p0-voice-reliability` → `G:\Javis` main**（merge --no-ff，逐提交 review）。
3. **G 盘构建 v3.0.1**，产出安装包 + `runtime-version.json`（含源码提交号 + 运行时 sha256）。
4. **D 盘安装验收**，逐条跑 P0 验收门槛，如实记录。
5. 验收通过后，启动 P2 记忆系统第一层（统一 MemoryManager + 治理 schema）。

> 本计划不臆造"完成"。每一步以真实运行结果为唯一事实来源；不能验证的项（如真实中文识别率）明确标注为"需 D 盘验收"，不宣称通过。
